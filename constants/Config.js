// Central Configuration for the App
// Update this IP once to change it everywhere in the app

const LOCAL_IP = "192.168.43.242"; // <-- Bas yahan change karna hai!
export const BASE_URL = `http://${LOCAL_IP}:3000/user`;

export default {
    BASE_URL,
    LOCAL_IP
};
